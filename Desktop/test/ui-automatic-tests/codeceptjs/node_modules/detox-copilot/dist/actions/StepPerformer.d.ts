import { PromptCreator } from '../utils/PromptCreator';
import { CodeEvaluator } from '../utils/CodeEvaluator';
import { SnapshotManager } from '../utils/SnapshotManager';
import { CacheHandler } from '../utils/CacheHandler';
import { CodeEvaluationResult, PreviousStep, PromptHandler } from '../types';
export declare class StepPerformer {
    private context;
    private promptCreator;
    private codeEvaluator;
    private snapshotManager;
    private promptHandler;
    private cacheHandler;
    constructor(context: any, promptCreator: PromptCreator, codeEvaluator: CodeEvaluator, snapshotManager: SnapshotManager, promptHandler: PromptHandler, cacheHandler: CacheHandler);
    private generateCacheKey;
    private captureSnapshotAndViewHierarchy;
    private shouldOverrideCache;
    private generateCode;
    perform(step: string, previous?: PreviousStep[], attempts?: number): Promise<CodeEvaluationResult>;
}
